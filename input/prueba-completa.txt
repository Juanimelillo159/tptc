/* ===== PRUEBAS COMPLETAS PARA COMPILADOR ===== */

/* Función main obligatoria - SIEMPRE PRIMERO */
int main() {
    /* ===== DECLARACIONES DE VARIABLES ===== */
    int a = 5;
    char c = 'A';
    double d = 3.14;
    int b = 1;
    
    /* ===== ASIGNACIONES COMPLEJAS ===== */
    a = 10;
    c = 'B';
    d = 2.5;
    b = 0;
    a = a + 3;
    a = a - 2;
    a = a * 2;
    a = a / 4;
    a = a % 3;
    
    /* ===== EXPRESIONES ARITMÉTICAS COMPLEJAS ===== */
    int suma = a + 2;
    int resta = a - 1;
    int mult = a * 3;
    int div = a / 2;
    int mod = a % 3;
    
    /* ===== EXPRESIONES LÓGICAS COMPLEJAS ===== */
    /* Eliminadas las asignaciones de bool a int */
    
    /* ===== IF-ELSE ANIDADOS ===== */
    if (a > 8) {
        suma = a + 1;
    } else {
        suma = a - 1;
    }
    
    if (b) {
        a = 100;
    } else {
        if (a == 10) {
            a = 50;
        } else {
            a = 25;
        }
    }
    
    /* IF anidado */
    if (a > 5) {
        if (b) {
            c = 'X';
        } else {
            c = 'Y';
        }
    }
    
    /* ===== WHILE ANIDADOS ===== */
    int i = 0;
    while (i < 3) {
        int j = 0;
        while (j < 2) {
            suma = suma + j;
            j = j + 1;
            if (j == 2) {
                /* continue eliminado - estaba fuera de contexto */
            }
        }
        suma = suma + i;
        i = i + 1;
        if (suma > 50) {
            break;  /* Este break está dentro del while, es válido */
        }
    }
    
    /* ===== FOR ANIDADOS ===== */
    int j;
    int k;
    for (j = 0; j < 3; j = j + 1) {
        for (k = 0; k < 2; k = k + 1) {
            suma = suma + k;
            /* continue eliminado - estaba en posición incorrecta */
        }
        suma = suma + j;
        if (suma > 100) {
            break;  /* Este break está dentro del for, es válido */
        }
    }
    
    /* ===== RETORNO ===== */
    return 0;
}